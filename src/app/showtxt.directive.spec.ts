import { ShowtxtDirective } from './showtxt.directive';

describe('ShowtxtDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowtxtDirective();
    expect(directive).toBeTruthy();
  });
});
